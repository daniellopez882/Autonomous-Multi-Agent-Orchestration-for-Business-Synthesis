# Runtime dependencies. Test and lint tooling lives in requirements-dev.txt.
#
# The previous file listed five bare package names, and shipped pytest into the
# runtime set.
python-dotenv>=1.0,<2.0
openai>=1.10,<3.0
anthropic>=0.18,<2.0
pydantic>=2.7,<3.0
fastapi>=0.111,<1.0
uvicorn[standard]>=0.29,<1.0
